import allRounds from '../assets/data/rounds/allRounds'

const defaultState = {
    
    curHovered: 'x100',
    curSelected: "",
    curRound:0,
    roundData: allRounds,
    simpleMode: false
}

export default defaultState;